#include <windows.h>

int WINAPI WinMain(HINSTANCE instance, HINSTANCE previous, LPSTR cmdline, int show) {
    MessageBoxA(NULL, "Slope sandbox visible sample executed", "Slope CTF sample", MB_OK | MB_TOPMOST);
    return 0;
}
